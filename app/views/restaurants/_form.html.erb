<div class = "container">
  <%= simple_form_for(restaurant) do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :phone_number %>
    <%= f.input :category, collection: Restaurant::FOODTYPE %>
    <%= f.button :submit, class: 'btn btn-primary w-100' %>
  <% end %>
</div>

